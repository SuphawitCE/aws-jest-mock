const {
  mockAnswer,
  mockPersonal,
} = require("./fixtures/userData/userDataOutput.json");

describe("user.js", () => {
  test("Should return firstname lastname and age", () => {
    // const user = jest.fn(() => )
    // expect()
  });
});
